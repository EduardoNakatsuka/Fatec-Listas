Procedimento: Procedimentos são estruturas que agrupam um conjunto
de comandos, que são executados quando o procedimento é chamado.

Parametro por valor: permite usar dentro de uma função uma cópia do valor de uma variável, porém não permite alterar o valor da variável original (somente a cópia pode ser alterada).

Parametro por referência: É passada para a função uma referência da variável, sendo possível alterar o conteúdo da variável original usando-se esta referência.

Variavel local: São aquelas declaradas dentro do bloco de uma função. Não podem ser usadas ou modificadas por outras funções. Somente existem enquanto a função onde foi declarada estiver sendo executada.

Variavel global:São declaradas fora de todos os blocos de funções. São acessíveis em qualquer parte do programa, ou seja, podem ser usadas e modificadas por todas as- outras funções. Existem durante toda a execução do programa